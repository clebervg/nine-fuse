name: nine_fuse
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Traduções: pacote do próprio SDK, sem dependência de terceiro. As chaves
  # viram getters tipados (ver l10n.yaml), então chave inexistente é erro de
  # compilação em vez de texto sumido na tela do jogador.
  flutter_localizations:
    sdk: flutter
  # Usada pelo código gerado do gen-l10n para plural e formatação de número.
  intl: ^0.20.2

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  flutter_animate: ^4.5.2
  gap: ^3.0.1
  shared_preferences: ^2.5.5
  # Renderiza o logo em SVG na UI: um só arquivo serve qualquer densidade de
  # tela, sem precisar de variantes @2x/@3x.
  flutter_svg: ^2.0.16
  google_mobile_ads: 9.0.0
  # Splash nativa de cold start (Android/iOS). Vai em `dependencies`, não em
  # `dev_dependencies`, porque expõe API de runtime (`FlutterNativeSplash.
  # preserve`/`remove`, chamada de `main.dart`) — diferente do
  # `flutter_launcher_icons`, que só roda como ferramenta de build e nunca é
  # importado pelo app. A configuração mora em `flutter_native_splash.yaml`,
  # não aqui, pelo mesmo motivo do arquivo do launcher_icons.
  flutter_native_splash: ^2.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Gera os ícones nativos de Android, iOS e Web a partir de assets/images/logo.png.
  # Só de desenvolvimento: roda uma vez (`dart run flutter_launcher_icons`) e o
  # resultado vai versionado, não é dependência de runtime.
  flutter_launcher_icons: ^0.14.4

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  image: ^4.9.1

# A configuração dos ícones NÃO mora aqui — mora em `flutter_launcher_icons.yaml`,
# que é o arquivo padrão do pacote e vence sobre este bloco quando os dois
# existem (`Config.loadConfigFromPath(defaultConfigFile)` antes do fallback para
# o pubspec). Havia um bloco duplicado aqui apontando `adaptive_icon_foreground`
# para o logo INTEIRO — com o fundo escuro arredondado dentro dele —, o que o
# Android encolheria de novo para a zona segura, deixando a marca minúscula numa
# ilha preta. Era configuração morta, mas morta e contraditória: quem a lesse
# acreditaria estar olhando a config em vigor.

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # Gera `AppLocalizations` a partir dos ARB de lib/l10n/ (ver l10n.yaml).
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # A licença acompanha os arquivos da fonte e é registrada em runtime
  # (ver core/theme/app_fonts.dart), como a OFL exige de quem redistribui.
  assets:
    - assets/fonts/OFL.txt
    - assets/images/

  # Nunito vem empacotada, não baixada em runtime: uma fonte que depende de
  # rede cai silenciosamente no padrão do sistema quando o aparelho está
  # offline, e o jogo muda de cara sem aviso.
  fonts:
    - family: Nunito
      fonts:
        - asset: assets/fonts/Nunito-Bold.ttf
          weight: 700
        - asset: assets/fonts/Nunito-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/Nunito-Black.ttf
          weight: 900

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
